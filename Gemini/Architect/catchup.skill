You are executing the /catchup skill for the Builds workspace.

## Purpose
Start every session with a 60-second briefing. Runs in an isolated sub-agent so that
SSH output, log data, and JSON state never pollute the main context window.
Only the final briefing enters the main session.

**Focus:** Developer workflow and active projects. Rapid2 checks run on-demand only
via `/status` — do not include trading bot data in the default catchup.

---

## Your job as the main agent

Do NOT run any SSH commands or read any files yourself. Instead:

1. Use the **Agent tool** (subagent_type="general-purpose") with the task prompt below.

2. When the sub-agent returns, output its briefing verbatim to the user.

3. If the briefing shows any errors or blockers, call those out explicitly before
continuing with work.

---

## Sub-agent task prompt (pass this to the Agent tool)

You are gathering a session briefing for a developer. Collect data from the sources
below, then produce a single compact summary. Do not show raw data — only the report.

**Important:** Do NOT run SSH commands or check trading bot status. This is a
developer-focused briefing. Use `/status` for Rapid2 bot health checks.

## Step 1 - Read memory files

Read both files in full:
- C:\\Users\\benja\\.claude\\projects\\c--Users-benja-OneDrive-Documents-Builds\\memory\\MEMORY.md
- C:\\Users\\benja\\.claude\\projects\\c--Users-benja-OneDrive-Documents-Builds\\memory\\rapid2.md

Note any **pending developer tasks** or **active projects** from MEMORY.md.
Skip Rapid2 operational details (those are in `/status`).

## Step 2 - Git status (workspace root)

Run:
  cd "c:/Users/benja/OneDrive/Documents/Builds" && git status --short && git log --oneline -3

Report:
- Uncommitted files (grouped by project if clear)
- Last 3 commit messages

## Step 3 - Check for active project context

Scan these directories and report what looks actively developed (recent .md or code files):
- c:/Users/benja/OneDrive/Documents/Builds/Architect/
- c:/Users/benja/OneDrive/Documents/Builds/Booksmut/
- c:/Users/benja/OneDrive/Documents/Builds/Rapid2/

## Step 4 - Produce the briefing

Return ONLY the report below. No preamble. No raw data. Just the formatted briefing.

--- SESSION CATCHUP ---
Date: [today's date]

MEMORY
  Active projects: [from MEMORY.md]
  Pending tasks: [from MEMORY.md]

GIT (workspace)
  Last commits: [last 3 messages]
  Uncommitted: [files or "clean"]

PROJECT PULSE
  Architect: [recent activity or "quiet"]
  Booksmut:  [recent activity or "quiet"]
  Rapid2:    [recent activity or "quiet" — use /status for bot health]

BLOCKERS
  [Any errors, conflicts, or issues found — or "none"]

---
**Quick commands:**
  `/status` — Rapid2 bot health and portfolio
  `/think`  — Plan a new task
  `/review` — Code review before deploy
  `/deploy` — Deploy to EC2
---
